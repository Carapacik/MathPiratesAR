# Example configuration

Vuforia and UFO license fields contain invalid example strings. Configure your own SDK license keys locally before using AR functionality. Unity service URLs have been replaced with example domains.

Values containing `EXAMPLE`, zero-only identifiers and `example.com` domains are placeholders, not real credentials. Never use these example credentials in production.
